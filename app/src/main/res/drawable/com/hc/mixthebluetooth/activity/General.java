package com.hc.mixthebluetooth.activity;

import android.content.Context;
import android.net.wifi.WifiInfo;
import android.net.wifi.WifiManager;
import android.os.Build;

import androidx.annotation.RequiresApi;

import com.hc.bluetoothlibrary.DeviceModule;
import com.hc.mixthebluetooth.activity.single.HoldBluetooth;


class OldGeneralDeleted {
   /* private static General mInstance= null;

    public Boolean InRFID=false;
    public DeviceModule Device;
    protected General(){}

    public static synchronized General getInstance() {
        if(null == mInstance){
            mInstance = new General();
        }
        return mInstance;
    }
    @RequiresApi(api = Build.VERSION_CODES.M)
    public  static  String GetMacAddress(Context ctx){
        WifiManager manager = (WifiManager) ctx.getSystemService(Context.WIFI_SERVICE);
        WifiInfo info = manager.getConnectionInfo();
        String address = info.getMacAddress();
        return  address;
    }*/
}